# AGENTS.md

## 身份与沟通

- 称呼用户为“花月哥哥”，默认使用中文。
- 先给结论和行动，再补充必要说明。
- 第一次调用工具前说明检查或执行内容；只在关键信息、方向变化或真实阻塞时汇报。

## 工作原则

- 先阅读真实代码、配置、数据库和测试，再修改。
- 只做完成当前验收所需的改动，不添加静默 fallback、假数据或兼容层。
- 修改持久化文件前创建清晰、可恢复的备份。
- 真实错误必须通过异常、非零退出码、失败测试或明确日志暴露。
- `sessions.db` 是事实源，`akasha.db` 是可确定性重建的派生状态。
- 在线增长和离线重放必须调用同一个 `MemoryCycle`，不得维护两套算法。
- 不使用 fast/slow/tag/reinforce 或外部确认信号改变图学习。

## 信任边界

- 在插件接口、事件、CLI、配置、SQLite 和 embedding 响应边界集中校验。
- 边界之后信任已经验证的 dataclass、schema 和领域不变量。
- 违反内部契约时 fail-fast、fail-loud；不以空列表、默认向量或跳过 turn 掩盖错误。
- 缺少 embedding 的完整 turn 仍以 BM25 和时间证据入库，并在索引元数据记录缺口；
  不得丢弃该 turn 或伪造向量。

## 代码风格

- 非平凡函数使用一句话 docstring 和编号阶段注释。
- 简单函数不强行添加 docstring 或编号注释。
- 函数通常不超过 50 行；阶段过多时按职责拆分。
- 函数、变量和模块名称表达计算职责，不使用脑区名称假装生物学一一对应。
- 流程图使用 Unicode box-drawing。

## 验证与交付

- 完成前运行直接相关的 unit、integration、parity 和真实重放验证。
- parity 必须比较真实召回集合和可审计状态，不能只报告 aggregate score。
- 私人 sessions、数据库和召回报告只放在 `private-data/`，不得提交。
- 测试失败或未运行时明确说明，不能声称完成。
